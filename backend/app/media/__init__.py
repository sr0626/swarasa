"""Dependency-free helpers shared between the FastAPI app and the resize
Lambda (app/lambda_handlers/resize_photo.py). See key_transform.py's own
docstring for why this package must stay stdlib-only.
"""
